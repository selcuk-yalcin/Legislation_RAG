# Railway Deployment - Minimal Requirements (Fast Build)
--extra-index-url https://download.pytorch.org/whl/cpu

# Core Dependencies
python-dotenv==1.0.0
numpy<2.0
httpx==0.27.0
openai==1.12.0

# Voyage AI (Embedding + Reranking)
voyageai==0.2.3

# MongoDB Vector Store
pymongo[srv]==4.6.1

# LangChain (Minimal - compatible versions)
langchain-text-splitters==0.3.11
langchain-core>=0.3.75,<2.0.0
langchain-community==0.3.31

# PDF Processing (only if needed for local preprocessing)
pypdf==3.17.4

# Web Server (Flask API)
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Arize Tracing (OpenRouter via OpenAI auto-instrumentation)
arize-otel>=0.7.0
openinference-instrumentation-openai>=0.1.0
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0

# Web Fallback Pipeline (Serper + Azure DI)
azure-ai-documentintelligence>=1.0.0
azure-core>=1.30.0
